apiVersion: 1

contactPoints:
  - orgId: 1
    name: controller-alerts
    receivers:
      - uid: controller_alerts
        name: controller alerts webhook
        type: webhook
        settings:
          url: http://localhost:4000/alerts/webhook?workspace=drycc
          httpMethod: POST
